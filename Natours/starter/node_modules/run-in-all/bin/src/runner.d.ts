declare type RunOptions = {
    isYarn: boolean;
    args: string;
    silent: boolean;
    parallel: boolean;
};
export declare function runCommandInDirs(command: string, directories: string[], options: RunOptions): Promise<number>;
export {};
