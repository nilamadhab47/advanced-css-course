export declare class Logger {
    log(data: string, context?: string): void;
    error(data: string, context?: string): void;
    private printMessage;
    private splitAndCleanLog;
}
declare const logger: Logger;
export default logger;
